window.onload = function() {
    //const
    const pi = 3.142;

    function calcArea(r) {
        const pi = 10;
        console.log("Calculation :" +
            pi * r * r);
    }

    console.log(pi);
    calcArea(5);
}